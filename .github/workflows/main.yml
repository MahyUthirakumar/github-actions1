name: example-basic
# This workflow represents a set of basic End-to-End tests
on:
  push:
    branches:
      - 'master'
  workflow_dispatch:
jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - name: Print
         run: echo "Hi"
